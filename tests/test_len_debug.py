# Debug test - let's see what happens with len lookup  
print("Testing len lookup...")
print("len value:", len)
print("len type:", type(len))

# Test a simple valid call
print("len([1]) =", len([1]))
